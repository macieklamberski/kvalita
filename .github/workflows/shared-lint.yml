name: Shared lint

on:
  workflow_call:
    inputs:
      bun-version:
        description: Bun version to install. Defaults to the latest release.
        required: false
        type: string
        default: latest
      prepare:
        description: Commands run after install, for repositories whose checks need generated files.
        required: false
        type: string
        default: ''

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ inputs.bun-version }}

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Prepare
        if: inputs.prepare != ''
        run: |
          ${{ inputs.prepare }}

      - name: Run Biome check
        run: bunx @biomejs/biome check --no-errors-on-unmatched --files-ignore-unknown=true .

      # --build so a root tsconfig.json that only lists project references is followed to
      # the projects themselves; plain --noEmit owns no files there and checks nothing.
      - name: Run TypeScript check
        if: hashFiles('tsconfig.json') != ''
        run: bunx tsc --build --noEmit

      # Dependabot writes a lower-case subject after the prefix and offers no option to
      # change it (dependabot/dependabot-core#11705), which the sentence-case rule rejects.
      # Only this step skips, so Biome and TypeScript still cover the lockfile it changed.
      - name: Validate PR commits
        if: github.event_name == 'pull_request' && github.actor != 'dependabot[bot]' && hashFiles('commitlint.json') != ''
        run: |
          bunx commitlint \
            --from ${{ github.event.pull_request.base.sha }} \
            --to ${{ github.event.pull_request.head.sha }} \
            --config ./commitlint.json \
            --verbose
